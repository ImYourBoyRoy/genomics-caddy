# ./.npmrc
# @sveltejs/kit still declares peerOptional typescript ^5.3 || ^6, while this
# project uses TypeScript 7 for tsc (svelte-check uses @typescript/typescript6).
# Until Kit widens its peer range, allow the known-safe mismatch.
legacy-peer-deps=true
